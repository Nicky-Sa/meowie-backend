1. Run `chmod +x configure-aws.sh`
2. Run `chmod +x installer.sh`
3. Run `./installer.sh`
4. Run `export AWS_KEY=<your-key> AWS_SECRET=<your-secret> AWS_REGION=<your-region>`
5. Run `./configure-aws.sh`